#include <stdio.h>

int main()
{
    int c, b;
    printf("Enter the number of c %d \n");
    scanf("%d", &c);
    printf("Enter the number of b %d\n");
    scanf("%d", &b);
    printf("the area of the rectangle is %d", c * b);
    return 0;
}