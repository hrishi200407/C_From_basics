#include <stdio.h>

int main()
{
    // type instructors**

    // int a=4,b,c;
    // printf("the value of a is %d", a);
     float a= 2.1;
     float b= a + 4.5;
    printf("the value of b is %f", b);
    // int a=4, j=8, k=9;
    // int sum= a+j+k;
    // printf("the value of sum is %d", sum);  


    //arithemic instructors**

    // printf("5 when divied by 2 leaves a remainder is %d", 5%2);




    return 0;
}